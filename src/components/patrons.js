import React from "react"
import { StaticQuery, graphql } from "gatsby"

import style from "./../styles/patrons.module.sass"

const Patrons = () => (
  <div className={style.patronsSplashContainer}>
    <h1>Coming soon</h1>
  </div>
)

export default Patrons
